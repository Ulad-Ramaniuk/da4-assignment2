## How to reproduce

1. Install dependencies:
```
pip install -r requirements.txt
```

2. Run the analysis:
```
python analysis.py
```

That's it. If `data/wdi_raw.csv` is not present, the script will automatically download all required data from the World Bank API. Output figures will be saved to `output/figures/`.# da4-assignment2
